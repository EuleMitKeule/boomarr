"""Configuration management module.

Handles loading, parsing, and validation of Boomarr configuration files.
Supports multiple configuration sources (files, environment variables) and
provides type-safe access to configuration values with sensible defaults.
"""
